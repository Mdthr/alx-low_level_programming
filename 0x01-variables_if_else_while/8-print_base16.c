#include<stdio.h>

/**
 * main - Entry Point
 *
 * Return: Always 0
 */

int main(void)
{
	char c;

	int a;

	c = 'a';
	a = 0;
	while
		(a < 10) {
			putchar(a);
			a++;
		}
	while
		(c <= 'f') {
			putchar(c);
			c++;
		}
	putchar('\n');
	return (0);

}
